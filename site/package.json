{
  "name": "free-router-site",
  "type": "module",
  "version": "0.1.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "node scripts/generate-site.mjs && vite",
    "generate": "node scripts/generate-site.mjs",
    "check": "tsc --noEmit",
    "build": "node scripts/build-site.mjs",
    "preview": "vite preview",
    "sync:manifest": "node scripts/generate-site.mjs --write-manifest"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^6.4.2"
  }
}
